// Isolated in its own file (no other imports) to avoid circular dependencies
// between utils/address.js and the api/cart layer.
export const ESTIMATE_PLACEHOLDER_FIRSTNAME = '__ESTIMATE_PLACEHOLDER__';
